# Metronome collection manifest - 2026-08-28T211435

- collected-changed: 82
- collected-new: 1
- unchanged: 145

Collection stops here. Ingest requires a separate user action.

Post-run correction: the revenue-recognition page is `unchanged`, not
`collected-changed`. Its comparison selected the separate
revenue-recognition-examples page because of a stem-prefix collision. Corrected
round totals are 81 `collected-changed`, 1 `collected-new`, and 146 `unchanged`;
the accepted duplicate raw snapshot and original run evidence are retained.
