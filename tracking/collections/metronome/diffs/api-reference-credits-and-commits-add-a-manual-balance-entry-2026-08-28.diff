--- raw/metronome/api-reference/credits-and-commits/add-a-manual-balance-entry-2026-07-13.md
+++ raw/metronome/api-reference/credits-and-commits/add-a-manual-balance-entry-2026-08-28.md
@@ -197,6 +197,8 @@
           description: >-
             RFC 3339 timestamp indicating when the manual adjustment takes
             place. If not provided, it will default to the start of the segment.
+        uniqueness_key:
+          $ref: '#/components/schemas/UniquenessKey'
     Error:
       required:
         - message
@@ -204,6 +206,14 @@
       properties:
         message:
           type: string
+    UniquenessKey:
+      type: string
+      minLength: 1
+      maxLength: 128
+      description: >-
+        Prevents the creation of duplicates. If a request to create a record is
+        made with a previously used uniqueness key, a new record will not be
+        created and the request will fail with a 409 error.
   responses:
     NotFound:
       description: The specified resource was not found
