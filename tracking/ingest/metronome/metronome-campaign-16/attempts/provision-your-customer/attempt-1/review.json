{
  "job_id": "provision-your-customer",
  "attempt": 1,
  "verdict": "changes_requested",
  "reason": "The raw SHA-256 and canonical URL match the immutable worker order; candidate.md equals receipt.source_page, suggestions.json equals receipt.suggestions, the path-qualified raw backlink resolves, the canonical source target is absent, all related wikilinks resolve, and all five excerpts byte-match the assigned raw source at their cited locations. The candidate correctly preserves the guide's `enabled` prose versus the example's `entitled` field without inventing an `enabled` request key, treats the 1,000 individual-seat statement as a default support boundary with an escalation path rather than a hard API maximum, and explicitly leaves exact proration formulas plus seat collision, reassignment, removal, and post-removal balance behavior unknown. Two semantic omissions prevent approval. First, the guide calls the unassigned-seat input `initial_unassigned_seats_quantity`, while the dedicated create-contract schema names it `initial_unassigned_seats`; the candidate instead says no contradiction exists and presents the guide spelling without warning. Second, the source adds durable `invoice_placement` behavior that is absent from the existing invoicing concept, but the suggestions omit both a fact-bearing metronome-invoicing update and its reciprocal source link. The seat prerequisite and seat-config section is also an important part of the candidate but is not represented in the retained grounding excerpts. These are source-context, concept-completeness, and evidence defects rather than mechanical prose issues, so the unchanged-hash retry requires another complete review.",
  "required_changes": [
    "Add an explicit documentation contradiction for the unassigned-seat field: this guide names `initial_unassigned_seats_quantity`, while the dedicated create-contract schema names `initial_unassigned_seats`. Remove or narrow the blanket no-contradiction statement, do not choose one spelling as current runtime truth without newer authority, and preserve the separate unknowns for seat reassignment, removal, collision handling, and post-removal balance lifecycle.",
    "Add a fact-bearing update for `wiki/concepts/metronome/metronome-invoicing.md` covering subscription `invoice_placement`: default `ON_USAGE_INVOICE` on the matching-billing-date usage invoice; `ON_SCHEDULED_INVOICE` appends to a same-date scheduled invoice or creates one when absent. Add the reciprocal source link and include `[[metronome-invoicing]]` in the candidate's Related concepts. Keep billing-provider routing conditional and do not infer invoice finalization, collection success, payment timing, or provider-delivery behavior.",
    "Expand or replace one retained excerpt so the grounding evidence includes the individual-seat prerequisites and contract configuration at raw lines 193-235: streaming-metric group-key immutability, product presentation-group-key dependency, event `seat_id`, stable unique identifiers, `SEAT_BASED`, and the guide's three `seat_config` labels. This evidence must accompany, not erase, the field-name contradiction and must not invent optionality or seat lifecycle behavior beyond the guide and dedicated schema."
  ],
  "review_scope": "full",
  "retry_review_scope": "full",
  "shared_update_decisions": [
    "provision-subscription-contract-mechanics",
    "provision-subscription-seat-credit-models",
    "provision-subscriptions-source-link",
    "provision-seat-credit-allocation",
    "provision-credits-source-link"
  ],
  "reviewer_identity": "/root/c16_r_provision",
  "reviewer_model": "gpt-5.6-sol"
}
