# Campaign 43 dispatch entry

Status: exact five-job manifest approved by the user on 2026-09-10; execution authorized under the current rules/psp/metronome-ingest.md and this contract.

On exact-manifest approval, adopt the current rules/psp/metronome-ingest.md with CLAUDE.md, rules/ingest.md, rules/psp/metronome.md, this campaign selection and unmodified trusted role orders. Do not load historical instruction chains. Use Sol medium workers, different Sol high initial reviewers, and up to three dynamic child slots bounded by actual runtime capacity. Persist and confirm each native dispatch; reconcile a rejected launch rather than duplicating its attempt. Do not create test agents or assume Done entries establish released capacity.

Apply the approved assertion-level retention test while drafting: select the right operation, explain its central effect, or prevent material misuse. Otherwise omit the assertion and provide useful verified raw navigation. No hard word cap, optional schema inventory, catalog of hypothetical unknowns, or extra shortening artifact. Preserve critical warnings and relevant contradictions, full pinned-raw reading, three-to-five precisely located quotes, raw provenance and independent initial review.

The ten predetermined questions form one final audit. Record one route per page and answer its two questions separately, preserving object/action checks, requested answers, exact evidence and individual verdicts. A where-to-find question needs a locator, not a schema inventory; a question requesting values still needs those values. Perform the gap sweep; select historical snapshots only when the question or a relevant conflict needs them, with the reason in existing extra-read notes. Every selected evidence file is read completely. No inference of historical equivalence from unread content. Handoff after one completeness check.

Coordinator alone promotes exact approved candidates and shared suggestions, then aggregates catalogs and runs one scoped mechanical/capsule close check. No new framework, result schema, reviewer waiver, retrospective rewrite, collection, push or subsequent campaign. Commit only if separately authorized.
