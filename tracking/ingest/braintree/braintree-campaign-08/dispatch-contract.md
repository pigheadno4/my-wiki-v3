# Braintree C08 dispatch contract

Status: COMPLETE; exact-manifest execution approved and completed on 2026-09-20. On approval,
this contract extends the existing Braintree parallel exception to C08 only.

Read CLAUDE.md, rules/ingest.md, rules/psp/braintree-ingest.md and adopted
rules/psp/metronome-ingest.md, then this selection-review.md and the unmodified
trusted order. Use braintree/braintree-campaign-08, never a previous campaign.

Keep one fully read raw per source, Sol medium workers, different Sol high initial
reviewers, three dynamic child slots, 3–5 located verbatim quotes and existing
result schemas. Coordinator alone writes repository/canonical/shared/runtime files;
workers/reviewers write only external handoffs. No semantic-review waiver.

Use the existing retrieval-oriented source scope. Keep central operation identity,
consequential warnings and useful raw-detail locators; do not copy API specifications.
Preserve 'typically' and recommendation language in the collected PCI warning.
Do not state that payment_method functions guarantee compliance or that every use
of a credit-card method always requires SAQ D. Retain nonce/raw-card distinctions
only as actually supported. Never fill missing rendered names or import neighboring
operations' destructive effects. Date inclusivity/timezone guarantees require actual
evidence. Unread references provide navigation only. Source assertions and concept
suggestions receive the same subject/condition/action self-check.

Use exact worker status candidate_ready, not completed, in the existing handoff
self-check. Reviewers apply the same narrow retrieval scope; optional schema details
are not blockers. Preserve uncertainty instead of guessing. Reuse original roles
for bounded corrections where available; maximum three attempts and full initial
review. Cross-source disputes require relevant evidence, not blanket neighbor reads.

Follow existing persisted-order review-priority dispatch with worker reservation.
Promote approved concept updates before corresponding exact sources, serially.
Dispatch fixed audit groups from selection-review.md promptly when promoted sources
are live. Auditors share the slot cap; no batch barrier or idle reserved reviewers.
Query audit uses actual index→concept→source→raw routes, full selected evidence,
existing gap sweep, object/action check, direct answer, locator and verdict.
One route per page, one shared completeness check, no report polishing.

Aggregate company/index/log/count once. Run one campaign mechanical close and typed
wiki validator, not the full unit suite for docs-only work. Keep actual timestamps
in existing artifacts, separating overlapping execution from delivery/closure lag.
No code/rule changes, new framework, raw edits, collection, GitHub ingest, commit,
push or further campaign authority. Raw immutability is provenance, not freshness
or correctness of upstream claims.
