# Braintree C07 dispatch contract

Status: COMPLETE; execution of this exact five-job manifest approved and completed on 2026-09-20.
On approval, this contract extends the Braintree parallel exception to C07 only.
It does not reopen C01–C06 or authorize bulk rollout.

Read CLAUDE.md, rules/ingest.md, rules/psp/braintree-ingest.md and its adopted
rules/psp/metronome-ingest.md, then this selection-review.md and the unmodified
trusted role order. Use selector braintree/braintree-campaign-07.

Keep Sol medium workers, different Sol high initial reviewers, three dynamic child
slots bounded by actual capacity, one complete raw per worker, 3–5 located exact
quotes and existing receipt schemas. Workers/reviewers are repository-read-only;
external handoffs only. Coordinator owns all canonical/shared/runtime writes.

Source is a retrieval entry, not an API specification. Preserve purpose, object,
SDK scope and consequential conditions; route routine fields/examples to verified
raw locations. Never infer missing semantics or import neighboring endpoint effects.
Address identity is customer-scoped; keep creation/update/deletion separate.
Preserve event-specific trigger/payload qualifications and distinguish plan listing
from creation or subscription behavior. Unread links provide navigation, not facts.
Concept suggestions default to useful reciprocal routes rather than copied detail.
Full-read concept audit still applies; provisional routes are not mandatory answers.

In the existing final worker self-check, verify subject/conditions/action and the
exact trusted result status `candidate_ready` (not `completed`). No new output
fields, checker or artifact. Reviewer applies the same retrieval scope, returning
all material blockers together. No optional specification expansion. Reuse original
roles for justified bounded corrections; maximum three attempts; first review is full.

Follow existing review-priority dynamic dispatch, worker reservation and persisted
trusted-order protocol. No idle reserved reviewers or batch barrier. On accepted
approval, promote supported concept updates then exact source serially. Once each
fixed audit group's sources are live, use an eligible slot immediately before
aggregate/report work; count auditors in the same three-slot limit. Audit groups
and exact ten questions are fixed in selection-review.md. Fully read selected raw,
perform existing query gap sweep, use one route per page and compact object/action,
answer, locator, verdict entries. One completeness check then handoff; no polishing.

Aggregate company/index/log/count once. Run one complete campaign mechanical close
check and touched typed-page validator, not a unit suite for documentation-only work.
Record actual start/analysis-end/handoff times in existing artifacts, distinguishing
overlap and unknown dispatch latency. Completed agents do not themselves prove
available capacity; reconcile actual dispatch failures without duplicate jobs.

No raw edits, collection, GitHub ingestion, code/rule changes, new scheduler,
registry, worktrees, reviewer waiver, commit/push or following campaign authority.
