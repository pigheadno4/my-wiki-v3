# Braintree C16 dispatch contract

Status: COMPLETE — exact C16 manifest execution approved and closed 2026-09-23.
Read `CLAUDE.md`, `rules/ingest.md`, `rules/psp/braintree-ingest.md`, adopted
`rules/psp/metronome-ingest.md`, this campaign's `selection-review.md` and the
unmodified trusted order before any role work. Each persisted order is
dispatched only through the approved campaign runtime.

Use the established C15 workflow without new machinery: three dynamic native
child slots beside the coordinator; Sol medium workers and different Sol high
initial reviewers; one complete pinned raw per job; 3–5 verbatim located
primary quotes; exact existing eight-key handoffs; repository-read-only role
agents; coordinator-only canonical, shared and tracking writes. No per-worker
worktrees, result-field changes, new registry or extra audit layer.

Write a retrieval entry, not a replacement operational manual. Retain purpose,
central operation/status, material eligibility or safety warning and verified
raw locators; leave routine tables, step-by-step settings and examples in raw.
Keep Control Panel versus API/SDK identity explicit. Do not turn an
authorization, settlement submission or pending status into final settlement
or funding. Treat the 2026-09-16 raw as collected evidence, not proof of a
current feature or support state. Read adjacent raw only for a retained
cross-source assertion or real conflict. Use `## Raw Sources` only for fully
read factual evidence and `## Related raw API references` for navigation-only
pages; never edit immutable raw for reverse links.

Reviewers fully read the assigned raw on first attempt and block misleading
claims, wrong action/scope, material warning omission and broken evidence or
reciprocal routes. Bounded corrections use targeted diff/context review only
when the effect is genuinely bounded; otherwise reread fully. Follow existing
review priority and worker reserve, with no batch barrier. Promote approved
concept updates before exact source candidates; company/index/log/count once
at close. Run the five fixed query groups and one campaign-scoped mechanical
close. No full code suite for documentation-only work.
