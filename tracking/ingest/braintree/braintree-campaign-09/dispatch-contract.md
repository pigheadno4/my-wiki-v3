# Braintree C09 dispatch contract

Status: COMPLETE; exact-manifest execution approved and completed on 2026-09-20. Approval
extends the existing Braintree parallel exception to C09 only; C01–C08 stay closed.

Read CLAUDE.md, rules/ingest.md, rules/psp/braintree-ingest.md, adopted
rules/psp/metronome-ingest.md, then selection-review.md and unmodified trusted
orders. Use selector braintree/braintree-campaign-09. Historical contracts are
not evidence or additional prerequisites.

Keep Sol medium workers, different Sol high initial reviewers, three dynamic child
slots bounded by actual capacity. One complete raw per source; 3–5 exact located
quotes; existing result schema and candidate_ready status. Workers/reviewers write
only external handoffs; coordinator alone writes canonical/shared/runtime files.

Source is a retrieval entry, not a replacement specification. Preserve central
purpose, recurring-billing enablement prerequisite, material update constraints
and source-supported boundaries. Route parameter inventories and repeated examples
to verified raw sections. Treat code/prose naming differences explicitly where
needed; never infer propagation to existing subscriptions from plan-update naming.
Keep listing add-ons/discounts distinct from creation or application. An unread
Ruby response link is navigation, not Node response evidence. Missing rendered
method names can be grounded in displayed code but not guessed. Investigate actual
conflicts with appropriate evidence, not every adjacent document by default.

Self-check retained source sentences AND concept suggestions for subject,
qualification and action using already-read evidence. Default concept change is
a useful reciprocal route, not copied fields. Full initial review uses the same
retrieval scope; optional detail is not a blocker. Return all material blockers
with concrete query impact in one pass. Reuse original roles for bounded targeted
corrections when unchanged evidence/prior findings/diff bound the impact; maximum
three attempts. No first-review waiver or coordinator semantic repair by default.

Persist each trusted order then dispatch promptly. Follow existing review priority
and worker reservation; no batch barrier or idle reserved reviewer. Reuse eligible
idle roles after capacity failures without duplicate work or weakened independence.
Promote each approved concept update then its exact source serially. Dispatch
selection-review's fixed audit groups once sources are live, before aggregate or
report work; auditors count against the same three slots.

Query audit: actual index→concept→source→raw route, full selected evidence, existing
gap sweep, object/action check, direct answer, verified locator and verdict. One
route/page, compact answers and one shared completeness check; no report polishing.
Group A and B together are one ten-question audit, not an extra full source review.

Aggregate company/index/log/count once and run one campaign mechanical close plus
touched typed-file validator. No unit suite for documentation-only execution.
Record actual timestamps and distinguish overlap/setup/message delay from compute.
No raw edits, collection, GitHub work, code/rule changes, new registry/classifier/
scheduler/worktree, commit/push, bulk rollout or following campaign authority.
