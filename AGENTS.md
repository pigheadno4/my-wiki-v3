# AGENTS.md

This project's instructions live in **[CLAUDE.md](CLAUDE.md)** (schema, conventions, domain guidance, and the Workflow Index) and the detailed workflow rules under **[rules/](rules/)**. Read `CLAUDE.md` first, then `Read` the specific `rules/*.md` file for whatever workflow you're about to perform — do not act before reading it.

There is intentionally no content here: `CLAUDE.md` is the single source of truth so the two files can never drift.
