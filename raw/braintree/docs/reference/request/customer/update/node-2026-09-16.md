<!-- Source URL: https://developer.paypal.com/braintree/docs/reference/request/customer/update/node -->
<!-- Fetched: 2026-09-16 -->
<!-- Discovery: llms.txt,sitemap.xml -->

---
title: Update
slug: /docs/reference/request/customer/update/node/
createTime: '2025-04-01T22:09:58.962Z'
updateTime: '2025-04-01T22:09:59.065Z'
---


# Customer: Update

See also the [Customer](/braintree/docs/reference/response/customer) response object.

To update a customer, use its ID along with new attributes. The same [validations](/braintree/docs/reference/general/validation-errors/all#customer) apply as when creating a customer. Any attribute not passed will remain unchanged.


### Node
```javascript
gateway.customer.update("theCustomerId", {
  firstName: "New First Name",
  lastName: "New Last Name"
}, (err, result) => {
});
```

## Examples


### Update customer and existing credit card

To update an existing credit card when using the customer update method, you need to pass the token of the credit card under the credit card options.

**NOTE**
Credit cards are the only payment method type that you can update using the customer update method. To update other payment method types, use. 

  
### Callback
```javascript

gateway.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com",
  creditCard: {
    options: { updateExistingToken: "theToken" }
  }
}, (err, result) => {
});

```

### Promise
```javascript

gateway.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com",
  creditCard: {
    options: { updateExistingToken: "theToken" }
  }
}).then(result => { });

```

If you are only storing the customer ID in your system and using a 1:1 model of customer to credit card, then you can get the token for the credit card from the customer details.

### Callback
```javascript

gateway.customer.find("theCustomerId", (err, customer) => {
  const token = customer.creditCards[0].token;
});

```

### Promise
```javascript

gateway.customer.find("theCustomerId").then(customer => {
  const token = customer.creditCards[0].token;
});

```


### Update customer, credit card, and billing address

The billing address can also be updated by adding in the billing address details and setting the `update_existing` option in the billing address attributes.

**NOTE**
Using this method, you can update any of the [credit card parameters listed above](#credit_card), but some parameters have been deprecated to avoid any PCI concerns with raw credit card data being present on your server.

 

  
### Callback
```javascript

braintree.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com",
  creditCard: {
    options: { updateExistingToken: "theToken" },
    billingAddress: {
      streetAddress: "New Street Address",
      postalCode: "60622",
      options: { updateExisting: true }
    }
  }
}, (err, result) => {
});

```

### Promise
```javascript

braintree.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com",
  creditCard: {
    options: { updateExistingToken: "theToken" },
    billingAddress: {
      streetAddress: "New Street Address",
      postalCode: "60622",
      options: { updateExisting: true }
    }
  }
}).then(result => { });

```

If you omit the`update_existing`valoption under the billing address, we will create a new address for the customer and associate it to the credit card. The old address will remain associated to the customer but no longer associated as the billing address of the credit card.
### Update customer and create new payment method

You can add a new payment method to an existing customer using the customer update API.


#### Credit card

If you omit the`update_existing_token`valoption from the examples in the previous sections, a new credit card will be created and associated to the customer.### Callback
```javascript

braintree.customer.find("theCustomerId").then(customer => {
    braintree.customer.update(customer.id, {
      paymentMethodNonce: nonceFromTheClient
    }).then(result => {
      result.customer.creditCards.length
      // 2
    });
  });

```


#### Any payment method type

You can use a payment method nonce for any payment method type—not just a credit card—to associate that payment method to the customer. The example below shows adding a payment method while also updating customer details.

### Callback
```javascript

gateway.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com"
}, (err, result) => {
});

```

### Promise
```javascript

gateway.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  email: "new.email@example.com"
}).then(result => { });

```


### Update default payment method

To update a customer's default payment method, get that payment method's[`token`](/braintree/docs/reference/response/payment-method/node#token)and pass it as the`default_payment_method_token`value below:### Callback
```javascript

braintree.customer.update("theCustomerId", {
  defaultPaymentMethodToken: "theToken",
}, (err, result) => {
});

```

### Promise
```javascript

braintree.customer.update("theCustomerId", {
  defaultPaymentMethodToken: "theToken",
}).then(result => { });

```


### Card verification

By default we will run[credit card validations](/braintree/docs/reference/general/validation-errors/all#credit-card)but not perform[verification](/braintree/docs/guides/credit-cards/server-side/node#card-verification). Braintree strongly recommends verifying all cards before they are stored in your Vault by[enabling card verification for your entire account](/braintree/articles/control-panel/vault/card-verification)in the Control Panel. If you choose to manually verify cards, set`verify_card`to`true`.### Callback
```javascript

braintree.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  creditCard: {
    options: {
      updateExistingToken: "theToken",
      verifyCard: true
    }
  }
}, (err, result) => {
});

```

### Promise
```javascript

braintree.customer.update("theCustomerId", {
  paymentMethodNonce: nonceFromTheClient,
  creditCard: {
    options: {
      updateExistingToken: "theToken",
      verifyCard: true
    }
  }
}).then(result => { });

```
  
**IMPORTANT**
If you are using our Premium Fraud Management Tools, we strongly recommend passing[`device_data`](/braintree/docs/guides/premium-fraud-management-tools/server-side/node#using-device-data)each time you verify a card.


## See also


- [Customer validations](/braintree/docs/reference/general/validation-errors/all#customer)
- [Payment method validations](/braintree/docs/reference/general/validation-errors/all#payment-method)
- [Card verification](/braintree/docs/guides/credit-cards/server-side/ruby#card-verification)

