<!-- Source URL: https://developer.paypal.com/braintree/docs/reference/request/subscription/search/node -->
<!-- Fetched: 2026-09-16 -->
<!-- Discovery: llms.txt,sitemap.xml -->

---
title: Search
slug: /docs/reference/request/subscription/search/node/
createTime: '2025-04-02T00:03:34.560Z'
updateTime: '2025-07-09T21:07:28.109Z'
---


# Subscription: Search


**NOTE**
Results are limited according to the [PayPal Data Protection Addendum For Card Processing Products policy](https://www.paypal.com/us/legalhub/braintree/data-protection-addendum) .

Returns a collection of[Subscription](/braintree/docs/reference/response/subscription)response objects.

You can search for subscriptions using a variety of criteria. For operators available on search fields, see the[search fields](/braintree/docs/reference/general/searching/search-fields)page.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.planId().is("the_plan_id");
}, (err, response) => {
    response.each((err, subscription) => {
        console.log(subscription.billingDayOfMonth);
    });
});
```
If you want to look up a single subscription using its ID, useinstead.
## Search results

All examples assume versionor greater. The search method returns a node stream and can be consumed as any other stream.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.price().between(5, 15);
});

stream.pipe(someWritableStream);
```

### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.price().between(5, 15);
});

stream.on("data", (subscription) => {
    // ...
});

stream.on("end", () => {
    // ...
});

stream.resume();
```
An example of searching prior to versioncan be found on the[search results](/braintree/docs/reference/general/searching/search-results#iterable-responses)page.
## Examples


### Searching by price

Searching on price uses a range field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.price().between(5, 15);
});
```

### Searching by plan ID

Searching on plan ID uses a multiple value field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.planId().is("goldPlan");
});
```

### Searching by status

Searching on status uses a multiple value field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.status().is(braintree.Subscription.Status.Active);
});
```
When searching for subscriptions that are active, you have the ability to search for all active subscriptions (with or without a trial period), subscriptions with a trial period, or subscriptions without a trial period.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.status().is(braintree.Subscription.Status.Active);
});
```

### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.status().is(braintree.Subscription.Status.Active);
    search.inTrialPeriod().is(true);
});
```

### Node
```javascript
const stream = gateway.subscription.search((search) => { search.status().is(braintree.Subscription.Status.Active); search.inTrialPeriod().is(false); });
```

### Searching by days past due

Searching on days past due uses a range field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.daysPastDue().min(42);
});
```

### Searching by merchant account ID

Searching on[merchant account ID](#merchant_account_id)acts like a multiple value field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.merchantAccountId().in("42", "43");
});
```

### Searching by billing cycles remaining

Searching on billing cycles remaining will find subscriptions which have a set limit to the number of times they will recur. It uses a range field.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.billingCyclesRemaining().between(1, 2);
});
```

### Searching by next billing date

Searching on next billing date will return subscriptions that will bill again during the date range you have given it. The example below will return any subscriptions that will be billed in the next five days.
### Node
```javascript
const today = new Date();
const fiveDaysFromNow = new Date();
fiveDaysFromNow.setDate(today.getDate() + 5);
const stream = gateway.subscription.search((search) => {
    search.nextBillingDate().max(fiveDaysFromNow);
});
```

### Searching by created-at date/time

Searching on created-at date/time will return subscriptions that were created during the range you have given.
### Node
```javascript
const today = new Date();
const yesterday = new Date();
yesterday.setDate(today.getDate() - 1);
const stream = gateway.subscription.search((search) => {
    search.createdAt().between(yesterday, today);
});
```

### Searching a combination

You can combine any of the search fields into a single search.
### Node
```javascript
const stream = gateway.subscription.search((search) => {
    search.planId().is("goldPlan");
    search.status().is(braintree.Subscription.Status.Active);
});
```

## See Also


- [Search fields](/braintree/docs/reference/general/searching/search-fields)
- [Search results](/braintree/docs/reference/general/searching/search-results)
- [Subscription object](/braintree/docs/reference/response/subscription)
- [Android Pay cards](/braintree/docs/reference/response/android-pay-card/ruby#subscriptions)
- [Apple Pay cards](/braintree/docs/reference/response/apple-pay-card/ruby#subscriptions)
- [Credit cards](/braintree/docs/reference/response/credit-card/ruby#subscriptions)
- [Google Pay cards](/braintree/docs/reference/response/google-pay-card/ruby#subscriptions)
- [PayPal accounts](/braintree/docs/reference/response/paypal-account/ruby#subscriptions)

