<!-- Source URL: https://developer.paypal.com/braintree/docs/reference/general/result-objects/node -->
<!-- Fetched: 2026-09-16 -->
<!-- Discovery: llms.txt,sitemap.xml -->

---
title: Result Objects
slug: /docs/reference/general/result-objects/node/
createTime: '2025-04-01T22:33:49.068Z'
updateTime: '2025-04-01T22:33:49.116Z'
---



# Result Objects

Result objects are wrapper objects that indicate whether or not the API call was a success and, if successful, include the requested data.
**NOTE**
API calls that don't have validations, such as searches, will return [a collection of requested objects](/braintree/docs/reference/general/searching/search-results)  instead of a result object.


## Success results

If the API call was successful, the `success` on the result will return`{true}`. The target object will be available as a member of the result object.
### Node.js
```javascript
result.success; // true
```

## Error results

If the API call was not successful, the success on the result will return`{false}`. An error may be due to:
- A[validation error](/braintree/docs/reference/general/validation-errors/overview)caused by invalid parameters
- A[processor decline](/braintree/docs/reference/general/statuses#processor-declined)or[gateway rejection](/braintree/docs/reference/general/statuses#gateway-rejected)
- Other[exceptional](/braintree/docs/reference/general/exceptions)conditions


### Node.js
```javascript
result.success; // false
result.errors.deepErrors();
```
The`errors`result object will only be populated if the error is due to a failed validation. In this case, the object will contain one or more[validation errors](/braintree/docs/reference/general/validation-errors/all)indicating which parameters were invalid:
### Callback
```javascript
gateway.customer.create({
    email: "invalidEmail",
    creditCard: {
        number: "notNumeric"
    }
}, (err, result) => {
    const deepErrors = result.errors.deepErrors();
    for (const i in deepErrors) {
        if (deepErrors.hasOwnProperty(i)) {
            console.log(deepErrors[i].attribute);
            console.log(deepErrors[i].code);
            console.log(deepErrors[i].message);
        }
    }
    const errors = result.errors;
    const customerErrors = errors.for("customer").deepErrors();
    for (const i in customerErrors) {
        if (customerErrors.hasOwnProperty(i)) {
            console.log(customerErrors[i].attribute);
            console.log(customerErrors[i].code);
            console.log(customerErrors[i].message);
        }
    }
    const creditCardErrors = errors.for("customer").for("creditCard").deepErrors();
    for (const i in creditCardErrors) {
        if (creditCardErrors.hasOwnProperty(i)) {
            console.log(creditCardErrors[i].attribute);
            console.log(creditCardErrors[i].code);
            console.log(creditCardErrors[i].message);
        }
    }
});
```

### Promise
```javascript
gateway.customer.create({
    email: "invalidEmail",
    creditCard: {
        number: "notNumeric"
    }
}).then(result => {
    const deepErrors = result.errors.deepErrors();
    for (const i in deepErrors) {
        if (deepErrors.hasOwnProperty(i)) {
            console.log(deepErrors[i].attribute);
            console.log(deepErrors[i].code);
            console.log(deepErrors[i].message);
        }
    }
    const errors = result.errors;
    const customerErrors = errors.for("customer").deepErrors();
    for (const i in customerErrors) {
        if (customerErrors.hasOwnProperty(i)) {
            console.log(customerErrors[i].attribute);
            console.log(customerErrors[i].code);
            console.log(customerErrors[i].message);
        }
    }
    const creditCardErrors = errors.for("customer").for("creditCard").deepErrors();
    for (const i in creditCardErrors) {
        if (creditCardErrors.hasOwnProperty(i)) {
            console.log(creditCardErrors[i].attribute);
            console.log(creditCardErrors[i].code);
            console.log(creditCardErrors[i].message);
        }
    }
});
```
For details on transaction error results,[see the transaction response object](/braintree/docs/reference/response/transaction/node#unsuccessful-result).
### Message

Themessageon the error result gives a human-readable description of what went wrong, regardless of the cause and nature of the error.
### Node.js
```javascript
console.log(result.message); // "Amount is required.\nCredit card number is invalid."
```
The message can contain multiple error messages.
**NOTE**
This was added in version {{sdkVersionForDate "Jul09_2010"}}


### Params

Error results include theparametersthat were submitted. This can be useful during Transparent Redirects to repopulate your form if validations fail.
### Node.js
```javascript
console.log(result.params); // {transaction: {creditCard: {expirationDate: {}}, amount: "10.00", type: "sale"}}
```
For PCI compliance reasons,credit card numberandcvvparameters are not included.
## See Also


- [Transaction Result Object](/braintree/docs/reference/response/transaction/ruby#result-object)
- [Braintree Marketplace Merchant Account Result Object](/braintree/docs/reference/response/merchant-account/ruby#result-object)
- [Validation Errors Overview](/braintree/docs/reference/general/validation-errors/overview)
- [Search Results](/braintree/docs/reference/general/searching/search-results)

