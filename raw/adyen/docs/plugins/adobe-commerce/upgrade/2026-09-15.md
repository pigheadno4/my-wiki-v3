<!-- Source URL: https://docs.adyen.com/plugins/adobe-commerce/upgrade.md -->
<!-- Fetched: 2026-09-15 -->
<!-- Discovery: llms.txt -->

---
title: "Upgrade the plugin"
url: "https://docs.adyen.com/plugins/adobe-commerce/upgrade"
source_url: "https://docs.adyen.com/plugins/adobe-commerce/upgrade.md"
canonical: "https://docs.adyen.com/plugins/adobe-commerce/upgrade"
last_modified: "2021-09-20T13:51:00+02:00"
language: "en"
---

# Upgrade the plugin

We recommend that you upgrade when we have a major release, such as [11.0.0](/plugins/release-notes/?version%5B0%5D=11.0.0).

We do not provide support for plugin versions earlier than [9.0.0](/plugins/release-notes/?plugin=magento_2\&version=8.0.0).

To stay up to date on the released fixes and features, subscribe to our [releases on GitHub](https://github.com/Adyen/adyen-magento2/releases).

The integration effort involved in upgrading depends on whether you have made any customizations to the plugin.

There are different steps to follow for:

* [Default integration](#default-integration)
* [Customized Integration](#customized-integration)

## Default Integration

1. In your command-line tool, go to the Adobe Commerce project root directory.

2. Run the following command:

   ```bash
     composer require adyen/module-payment:VERSION_TO_UPGRADE
   ```

   For example, running `composer require adyen/module-payment:11.0.0` will upgrade the plugin to v11.0.0.

3. Run the database migration scripts:
   ```bash
     bin/magento setup:upgrade
   ```

4. Clear the cache:
   ```bash
     bin/magento cache:flush
   ```

## Customized Integration

1. Extract any custom code you have added to the plugin.

2. In your command-line tool, go to the Adobe Commerce project root directory.

3. Run the following command:

   ```bash
     composer require adyen/module-payment:VERSION_TO_UPGRADE
   ```

   For example, running `composer require adyen/module-payment:11.0.0` will upgrade the plugin to v11.0.0.

4. Run the database migration scripts:
   ```bash
     bin/magento setup:upgrade
   ```

5. Clear the cache:
   ```bash
     bin/magento cache:flush
   ```

6. Check if the custom code is compatible with the new version and apply any changes if needed.
