<!-- Source URL: https://docs.adyen.com/point-of-sale/automating-terminal-management/configure-terminals-api.md -->
<!-- Fetched: 2026-09-15 -->
<!-- Discovery: llms.txt -->

---
title: "Use API calls to configure features"
description: "Configure features using our Management API."
url: "https://docs.adyen.com/point-of-sale/automating-terminal-management/configure-terminals-api"
source_url: "https://docs.adyen.com/point-of-sale/automating-terminal-management/configure-terminals-api.md"
canonical: "https://docs.adyen.com/point-of-sale/automating-terminal-management/configure-terminals-api"
last_modified: "2026-05-04T10:29:00+02:00"
language: "en"
---

# Use API calls to configure features

Configure features using our Management API.

The Management API lets you configure features at various levels using API requests instead of the [Customer Area](https://ca-test.adyen.com/).

## Requirements

Before you begin, take into account the following requirements, limitations, and preparations.

| Requirement                                                   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Integration type**                                          | A point-of-sale integration with Adyen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **[API credentials](/development-resources/api-credentials)** | You must have an API credential with an API key and the following [roles](/development-resources/api-credentials#manage-api-permissions):- Management API—Terminal settings read
- Management API—Terminal settings read and write
- For [sensitive terminal settings](#sensitive-terminal-settings): Management API—Terminal Advanced settings read and write If you have a Terminal API integration with [cloud-based communications](/point-of-sale/design-your-integration/choose-your-architecture#cloud-communications), you can use the existing API key that you use for Terminal API requests. |
| **Limitations**                                               | Note the following:- See the list of features that are [configurable](#configurable-features) using the Management API.
- Requests to live Management API endpoints related to terminal settings are subject to [rate limits](/point-of-sale/automating-terminal-management#rate-limits-in-the-live-environment).                                                                                                                                                                                                                                                                                       |

## Configurable features

You can configure features by making API requests to the following endpoints on a company, merchant, store, or terminal level:

* Terminal logos are under a dedicated `/terminalLogos` endpoint.
* The other settings are under a `/terminalSettings` endpoint.

Most settings from the following table link to the company account level as an example, but are also available on the other levels.

| Settings object                                                                                                                                           | Description                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [deviceName](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-signature-deviceName)           | Custom name that is used to identify the terminal.                                                                                                                                                                                                                                                                                                                                              |
| [cardholderReceipt](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-cardholderReceipt)       | Customize the structure of the header to show on the shopper receipt by defining the order of one or two header lines and blank lines. For example, **header1,header2,filler**. The text of the header lines is defined in the [Customer Area](https://ca-test.adyen.com/) under **Devices** > **Device settings** > **Payment features** > **Receipts** in the **Receipt header lines** block. |
| [connectivity](https://docs.adyen.com/api-explorer/Management/latest/patch/terminals/\(terminalId\)/terminalSettings#request-connectivity)                | `simcardStatus`: activate the SIM card in a specific payment terminal. This setting is only available at the terminal level.                                                                                                                                                                                                                                                                    |
| [gratuities](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-gratuities)                     | Enable tipping from the terminal, and predefine tipping options.                                                                                                                                                                                                                                                                                                                                |
| [hardware](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-hardware)                         | `displayMaximumBacklight`: set the brightness level of the terminal display.                                                                                                                                                                                                                                                                                                                    |
| [offlineProcessing](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-offlineProcessing)       | Set the chip floor limit for [offline EMV payments](/point-of-sale/offline-payment#enabling-offline-payments).                                                                                                                                                                                                                                                                                  |
| [opi](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-opi)                                   | Enable Pay at Table (`enablePayAtTable`), and set the Pay at Table endpoint URL (`payAtTableURL`) and store number (`payAtTableStoreNumber`). These settings are for OPI integrations.                                                                                                                                                                                                          |
| [receiptOptions](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-receiptOptions)             | Add your logo to the receipt. Specify data to print on the receipt as a QR code.                                                                                                                                                                                                                                                                                                                |
| [receiptPrinting](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-receiptPrinting)           | Determine whether the terminal should print a merchant receipt and/or a shopper receipt for certain transaction outcomes.                                                                                                                                                                                                                                                                       |
| [signature](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-signature)                       | Skip asking for a signature, considering that all global card schemes (American Express, China UnionPay, Diners, Discover, JCB, MasterCard, and Visa) regard a signature as optional. Determine whether you want the shopper to sign on the terminal display or on the receipt if you choose not to skip the signature.                                                                         |
| [standalone](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-standalone)                     | Set up the standalone feature and the default currency for payment terminals.                                                                                                                                                                                                                                                                                                                   |
| [storeAndForward](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-storeAndForward)           | Configure settings for [store and forward](/point-of-sale/offline-payment/#types-of-offline-payment) offline payments.                                                                                                                                                                                                                                                                          |
| [surcharge](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-surcharge)                       | Configure passing on scheme fees as a [surcharge](/point-of-sale/surcharge).                                                                                                                                                                                                                                                                                                                    |
| [tapToPay](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-tapToPay)                         | Configure settings for [Tap to Pay](/point-of-sale/mobile-ios/build/tap-to-pay/), for example the text shown on the screen of your mobile device. This setting is not available at the terminal level.                                                                                                                                                                                          |
| [terminalInstructions](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-terminalInstructions) | Configure the behaviour of the payment terminal, for example when the configuration update happens.                                                                                                                                                                                                                                                                                             |
| [timeouts](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-timeouts)                         | Set the amount of time after which the terminal goes to [sleep](/point-of-sale/managing-terminals/battery-management#sleep-mode) due to inactivity.                                                                                                                                                                                                                                             |
| [logos](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalLogos#request-data)                                   | Upload a logo to use as the terminal standby screen.                                                                                                                                                                                                                                                                                                                                            |

### Sensitive terminal settings

Some terminal settings are considered sensitive because they can affect the Terminal API communications. Sensitive terminal settings are under a `/terminalSettings` endpoint on a company, merchant, store, or terminal level.

To authenticate your requests to these, you need to have the following [role](/development-resources/api-credentials#manage-api-permissions):

* **Management API—Terminal Advanced settings read and write**

| Settings object                                                                                                                           | Description                                                                                                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| [nexo](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-nexo)                 | For local Terminal API integrations: specify event and display notifications URLs, and provide the key to secure local communications. |
| [passcodes](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-passcodes)       | Manage passcodes used to access payment terminal menus and features.                                                                   |
| [wifiProfiles](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings#request-wifiProfiles) | Provide properties of the Wi-Fi network the terminal should connect to. The terminal will connect to this network after boarding.      |

If you need to define more settings, you have to go through the [Customer Area](https://ca-test.adyen.com/).

## Inheritance of settings

You can define terminal settings at different levels. From high to low:

* Company account level
* Merchant account level
* Store level
* Terminal level

The top level, above the company level, is the Adyen level. This is known as the *PSP level*. You cannot define settings at PSP level.

Settings at the company level apply to all terminals under the company. Settings at the merchant account level override settings inherited from the company level and apply to all terminals under the merchant account, and so on.

In other words: settings defined at a certain level propagate to all terminals at and under that level. But settings defined at a lower level replace values inherited from a higher level.

## Get current settings

To view the terminal configuration at a specific level:

1. Get the ID of the account, store, or terminal you want to configure terminal settings for. You need to specify that ID in the URL of later requests.

   * For the company ID, make a GET [/companies](https://docs.adyen.com/api-explorer/Management/latest/get/companies) request.
   * For the merchant ID, make a GET [/merchants](https://docs.adyen.com/api-explorer/Management/latest/get/merchants) request.
   * For the store ID, make a GET [/stores](https://docs.adyen.com/api-explorer/Management/latest/get/stores) request or a GET [/merchants/{merchantId}/stores](https://docs.adyen.com/api-explorer/Management/latest/get/merchants/\(merchantId\)/stores) request.
   * For the terminal ID, make a GET [/terminals](https://docs.adyen.com/api-explorer/Management/latest/get/terminals) request.

   **Get merchant ID**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/merchants \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X GET
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);
   // Make the request
   AccountMerchantLevelApi service = new AccountMerchantLevelApi(client);
   ListMerchantResponse response = service.listMerchantAccounts(1, 1, null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Service\Management\AccountMerchantLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);

   $requestOptions['queryParams'] = array('pageNumber' => 'integer', 'pageSize' => 'integer');

   // Make the request
   $service = new AccountMerchantLevelApi($client);
   $response = $service->listMerchantAccounts($requestOptions);
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Make the request
   var service = new AccountMerchantLevelService(client);
   var response = service.ListMerchantAccounts(pageNumber: 1, pageSize: 1);
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.AccountMerchantLevelApi.listMerchantAccounts(1, 1);
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Make the request
   service := client.Management()
   req := service.AccountMerchantLevelApi.ListMerchantAccountsInput()
   req = req.PageNumber(1).PageSize(1)res, httpRes, err := service.AccountMerchantLevelApi.ListMerchantAccounts(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   query_parameters = {
     "pageNumber" : "integer",
     "pageSize" : "integer"
   }

   result = adyen.management.account_merchant_level_api.list_merchant_accounts(query_parameters=query_parameters)
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   query_params = {
     :pageNumber => 'integer',
     :pageSize => 'integer'
   }

   result = adyen.management.account_merchant_level_api.list_merchant_accounts(query_params: query_params)
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.AccountMerchantLevelApi.listMerchantAccounts(1, 1);
   ```

2. **To retrieve the terminal logo**, make a GET request to the `/terminalLogos` endpoint for the account, store, or terminal. You can retrieve only one logo at a time. On the account or store level, you need to specify the terminal model as a query parameter. On the terminal level no model query parameter is needed because we can infer the model from the terminal ID in the URL.

   Use the following endpoints:

   * GET [/companies/{companyId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/get/companies/\(companyId\)/terminalLogos)
   * GET [/merchants/{merchantId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/get/merchants/\(merchantId\)/terminalLogos)
   * GET [/stores/{storeId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/get/stores/\(storeId\)/terminalLogos)
   * GET [/terminals/{terminalId}/terminalLogos](https://docs.adyen.com/api-explorer/Management/latest/get/terminals/\(terminalId\)/terminalLogos)

   **Get merchant account logo for V400m terminal**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/merchants/{merchantId}/terminalLogos?model=V400m \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X GET
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);
   // Make the request
   TerminalSettingsMerchantLevelApi service = new TerminalSettingsMerchantLevelApi(client);
   Logo response = service.getTerminalLogo("merchantId", "String", null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Service\Management\TerminalSettingsMerchantLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);

   $requestOptions['queryParams'] = array('model' => 'string');

   // Make the request
   $service = new TerminalSettingsMerchantLevelApi($client);
   $response = $service->getTerminalLogo('merchantId', $requestOptions);
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Make the request
   var service = new TerminalSettingsMerchantLevelService(client);
   var response = service.GetTerminalLogo("merchantId", model: "string");
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsMerchantLevelApi.getTerminalLogo("merchantId", "string");
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Make the request
   service := client.Management()
   req := service.TerminalSettingsMerchantLevelApi.GetTerminalLogoInput("merchantId")
   req = req.Model("string")res, httpRes, err := service.TerminalSettingsMerchantLevelApi.GetTerminalLogo(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   query_parameters = {
     "model" : "string"
   }

   result = adyen.management.terminal_settings_merchant_level_api.get_terminal_logo(merchantId="merchantId", query_parameters=query_parameters)
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   query_params = {
     :model => 'string'
   }

   result = adyen.management.terminal_settings_merchant_level_api.get_terminal_logo('merchantId', query_params: query_params)
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsMerchantLevelApi.getTerminalLogo("merchantId", "string");
   ```

3. When you receive the response, Base64-decode the [data](https://docs.adyen.com/api-explorer/Management/latest/get/merchants/\(merchantId\)/terminalLogos#responses-200-data) string to get the image file of the logo.

   **Response showing current logo**

   ```json
   {
     "data": "R0lGODlh...AAAcDgH+BADs="
   }
   ```

4. **To retrieve the other terminal settings** (except the logo), make a GET request to the `/terminalSettings` endpoint for the account, store, or terminal.

   Use the following endpoints:

   * GET [/companies/{companyId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/get/companies/\(companyId\)/terminalSettings)
   * GET [/merchants/{merchantId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/get/merchants/\(merchantId\)/terminalSettings)
   * GET [/stores/{storeId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/get/stores/\(storeId\)/terminalSettings)
   * GET [/terminals/{terminalId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/get/terminals/\(terminalId\)/terminalSettings)

   **Get merchant account terminal settings**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/merchants/{merchantId}/terminalSettings \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X GET
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);
   // Make the request
   TerminalSettingsMerchantLevelApi service = new TerminalSettingsMerchantLevelApi(client);
   TerminalSettings response = service.getTerminalSettings("merchantId", null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Service\Management\TerminalSettingsMerchantLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);

   // Make the request
   $service = new TerminalSettingsMerchantLevelApi($client);
   $response = $service->getTerminalSettings('merchantId');
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Make the request
   var service = new TerminalSettingsMerchantLevelService(client);
   var response = service.GetTerminalSettings("merchantId");
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsMerchantLevelApi.getTerminalSettings("merchantId");
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Make the request
   service := client.Management()
   req := service.TerminalSettingsMerchantLevelApi.GetTerminalSettingsInput("merchantId")
   res, httpRes, err := service.TerminalSettingsMerchantLevelApi.GetTerminalSettings(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   result = adyen.management.terminal_settings_merchant_level_api.get_terminal_settings(merchantId="merchantId")
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   result = adyen.management.terminal_settings_merchant_level_api.get_terminal_settings('merchantId')
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsMerchantLevelApi.getTerminalSettings("merchantId");
   ```

   The response returns the current configuration.

   **Response showing current settings**

   ```json
   {
       "cardholderReceipt":{
           "headerForAuthorizedReceipt":"header1,header2,filler"
       },
       "gratuities":[
           {
               "currency":"EUR",
               "usePredefinedTipEntries":true,
               "predefinedTipEntries":[
                   "100",
                   "1%",
                   "5%"
               ],
               "allowCustomAmount":true
           }
       ],
       "nexo":{
           "displayUrls":{
               "localUrls":[
                   {
                       "password":"YOUR_PASSWORD",
                       "url":"https://your-display-notifications-endpoint.com",
                       "username":"USERNAME"
                   }
               ]
           },
           "encryptionKey":{
               "identifier":"SHARED_KEY",
               "passphrase":"PASSPHRASE",
               "version":1
           },
           "eventUrls":{
               "eventPublicUrls":[
                   {
                       "password":"YOUR_PASSWORD",
                       "url":"https://your-event-notifications-endpoint.com",
                       "username":"USERNAME"
                   }
               ]
           }
       },
       "opi":{
           "enablePayAtTable":true,
           "payAtTableStoreNumber":"1",
           "payAtTableURL":"https:/your-pay-at-table-endpoint.com"
       },
       "offlineProcessing":{
           "chipFloorLimit":0
       },
       "receiptOptions":{
           "qrCodeData":"http://www.example.com/order/${pspreference}/${merchantreference}"
       },
       "receiptPrinting":{
           "shopperApproved":true,
           "shopperRefused":true,
           "shopperCancelled":true,
           "shopperRefundApproved":true,
           "shopperRefundRefused":true,
           "shopperVoid":true
       },
       "signature":{
           "askSignatureOnScreen":true,
           "skipSignature":false,
           "deviceName":"Amsterdam-236203386"
       },
       "wifiProfiles":{
           "profiles":[
               {
                   "authType":"wpa-psk",
                   "autoWifi":false,
                   "bssType":"infra",
                   "channel":0,
                   "defaultProfile":true,
                   "hiddenSsid":false,
                   "name":"Guest Wi-Fi",
                   "psk":"4R8R2R3V456X",
                   "ssid":"G470P37660D4G",
                   "wsec":"ccmp"
               }
           ],
           "settings":{
               "band":"All",
               "roaming":true
           }
       },
       "timeouts":{
           "fromActiveToSleep":30
       },
       "hardware":{
           "displayMaximumBackLight":75
       },
       "passcodes":{
           "adminMenuPin":"1234",
           "txMenuPin":"1234",
           "screenLockPin":"1234"
       },
       "storeAndForward":{
           "maxAmount":[
               {
                   "amount":10000,
                   "currencyCode":"EUR"
               }
           ],
           "maxPayments":10,
           "supportedCardTypes":{
               "credit":true,
               "debit":true,
               "deferredDebit":true,
               "prepaid":true,
               "unknown":false
           }
       },
       "terminalInstructions":{
           "adyenAppRestart":true
       }
   }
   ```

## Update settings

Settings are parameters grouped in objects.

* To change a parameter value, include the full object that contains the parameter, even if you do not want to change all parameters in the object.
* To remove a parameter value at a certain level and restore the value inherited from the next higher level, include the full object that contains the parameter, and specify an empty value for the parameter or omit the parameter.
* Objects that are not included in the request are not updated.

If you omit a parameter from an object, that setting is removed.

In the next sections we describe how to:

* [Specify or remove a terminal logo](#update-logo)
* [Specify or remove other terminal settings](#update-other-settings)

### Update terminal logo

1. Make sure that you know:

   * The [level](#inheritance) where you need to add or remove a logo to get the desired effect.

   * The [current logo](#view-current-settings) at that level. When removing a logo, you also need to know the logo that will be inherited from the next higher level.

   * The terminal model that you want to change the logo for.

2. For a new logo, make sure the file matches the logo requirements for the terminal model, and convert the file to a Base64-encoded string.

   | `model`   | Logo dimensions width x height | File size         | Supports GIF BMP JPG JPEG PNG                                                               |
   | --------- | ------------------------------ | ----------------- | ------------------------------------------------------------------------------------------- |
   | AMS1      | 480 x 800                      | Max. 1 MB         | GIF and PNG                                                                                 |
   | E280      | 320 x 444                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | E285      | 240 x 284                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | E285p     | 240 x 284                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | E355      | 320 x 150                      | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | M400      | 854 x 432                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | MX925     | 800 x 443                      | Max. 500 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | P400Plus  | 320 x 450                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1E       | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1E2      | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1E2L     | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1EL      | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1F2      | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1F2L     | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | S1U       | 720 x 1280                     | Max. 1 MB         | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | V200cPlus | -                              | -                 | -                                                                                           |
   | V240m     | 320 x 450                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | V240mPlus | 320 x 450                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | V400cPlus | 320 x 450                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | V400m     | 320 x 450                      | Max. 512 KB       | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | VX680     | 240 x 200                      | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | VX690     | 240 x 200                      | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | VX820     | 240 x 200                      | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | UX300     | 128 x 42                       | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | UX301     | 128 x 42                       | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |
   | UX410     | 128 x 42                       | Validation on WxH | ![-white\_check\_mark-](/user/data/smileys/emoji/white_check_mark.png "-white_check_mark-") |

   If the dimensions of the logo do not match the requirements:

   * BMP, JPG, JPEG, or PNG files are resized.
   * A GIF file is converted to a single-frame image.

3. Make a PATCH request to the `/terminalLogos` endpoint for the account, store, or terminal. You can update only one logo at a time. On the account or store level, you need to specify the terminal model as a query parameter. On the terminal level no model query parameter is needed because we can infer the model from the terminal ID in the URL.

   Use the following endpoints:

   * PATCH [/companies/{companyId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalLogos)
   * PATCH [/merchants/{merchantId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/patch/merchants/\(merchantId\)/terminalLogos)
   * PATCH [/stores/{storeId}/terminalLogos?model={model}](https://docs.adyen.com/api-explorer/Management/latest/patch/stores/\(storeId\)/terminalLogos)
   * PATCH [/terminals/{terminalId}/terminalLogos](https://docs.adyen.com/api-explorer/Management/latest/patch/terminals/\(terminalId\)/terminalLogos)

   The request body must contain:

   * [data](https://docs.adyen.com/api-explorer/Management/latest/patch/stores/\(storeId\)/terminalLogos#request-data): to upload a logo, specify the Base64-encoded string of the logo file. To remove a logo, specify an empty string.

   For example, to upload a new terminal logo for use in a store:

   **Upload new store logo**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/stores/{storeId}/terminalLogos?model=V400m \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X PATCH \
   -d '{
        "data": "Z3lFPDxt...AAAcDgH+BADs="
   }'
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.model.management.*;
   import java.time.OffsetDateTime;
   import java.util.*;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);

   // Request objects
   Logo logo = new Logo()
   	.data("Z3lFPDxt...AAAcDgH+BADs=");

   // Make the request
   TerminalSettingsStoreLevelApi service = new TerminalSettingsStoreLevelApi(client);
   Logo response = service.updateTerminalLogoByStoreId("storeId", logo, "String", null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Model\Management\Logo;
   use Adyen\Service\Management\TerminalSettingsStoreLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);


   // Request objects
   $logo = new Logo();
   $logo
   	->setData("Z3lFPDxt...AAAcDgH+BADs=");

   $requestOptions['queryParams'] = array('model' => 'string');

   // Make the request
   $service = new TerminalSettingsStoreLevelApi($client);
   $response = $service->updateTerminalLogoByStoreId('storeId', $logo, $requestOptions);
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Model;
   using Adyen.Model.Management;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Fill in your request objects
   Logo logo = new Logo
   {
   	Data = "Z3lFPDxt...AAAcDgH+BADs="
   };

   // Make the request
   var service = new TerminalSettingsStoreLevelService(client);
   var response = service.UpdateTerminalLogoByStoreId("storeId", logo, model: "string");
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request object
   const logo = {
     data: "Z3lFPDxt...AAAcDgH+BADs="
   }

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalLogoByStoreId("storeId", logo, "string");
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Fill in your request objects
   logo := management.Logo{
   	Data: common.PtrString("Z3lFPDxt...AAAcDgH+BADs="),
   }

   // Make the request
   service := client.Management()
   req := service.TerminalSettingsStoreLevelApi.UpdateTerminalLogoByStoreIdInput("storeId").Logo(logo)
   req = req.Model("string")res, httpRes, err := service.TerminalSettingsStoreLevelApi.UpdateTerminalLogoByStoreId(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   json_request = {
     "data": "Z3lFPDxt...AAAcDgH+BADs="
   }

   query_parameters = {
     "model" : "string"
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_logo_by_store_id(request=json_request, storeId="storeId", query_parameters=query_parameters)
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   request_body = {
     :data => 'Z3lFPDxt...AAAcDgH+BADs='
   }

   query_params = {
     :model => 'string'
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_logo_by_store_id(request_body, 'storeId', query_params: query_params)
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request objects
   const logo: Types.management.Logo = {
   	data: "Z3lFPDxt...AAAcDgH+BADs="
   };

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalLogoByStoreId("storeId", logo, "string");
   ```

   The response returns the logo data at the level where you made the request.

   When you remove a terminal logo, the terminal inherits a logo from a higher level and the response returns that other logo. For example, let's assume you changed your mind and want to replace the store logo you just added with the logo defined at the merchant account level. You'd make a PATCH `merchants/{merchantId}/stores/{storeId}/terminalLogos?model={model}` request:

   **Remove store logo**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/stores/{storeId}/terminalLogos?model=V400m \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X PATCH \
   -d '{
        "data": ""
   }'
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.model.management.*;
   import java.time.OffsetDateTime;
   import java.util.*;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);

   // Request objects
   Logo logo = new Logo()
   	.data("");

   // Make the request
   TerminalSettingsStoreLevelApi service = new TerminalSettingsStoreLevelApi(client);
   Logo response = service.updateTerminalLogoByStoreId("storeId", logo, "String", null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Model\Management\Logo;
   use Adyen\Service\Management\TerminalSettingsStoreLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);


   // Request objects
   $logo = new Logo();
   $logo
   	->setData("");

   $requestOptions['queryParams'] = array('model' => 'string');

   // Make the request
   $service = new TerminalSettingsStoreLevelApi($client);
   $response = $service->updateTerminalLogoByStoreId('storeId', $logo, $requestOptions);
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Model;
   using Adyen.Model.Management;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Fill in your request objects
   Logo logo = new Logo
   {
   	Data = ""
   };

   // Make the request
   var service = new TerminalSettingsStoreLevelService(client);
   var response = service.UpdateTerminalLogoByStoreId("storeId", logo, model: "string");
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request object
   const logo = {
     data: ""
   }

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalLogoByStoreId("storeId", logo, "string");
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Fill in your request objects
   logo := management.Logo{
   	Data: common.PtrString(""),
   }

   // Make the request
   service := client.Management()
   req := service.TerminalSettingsStoreLevelApi.UpdateTerminalLogoByStoreIdInput("storeId").Logo(logo)
   req = req.Model("string")res, httpRes, err := service.TerminalSettingsStoreLevelApi.UpdateTerminalLogoByStoreId(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   json_request = {
     "data": ""
   }

   query_parameters = {
     "model" : "string"
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_logo_by_store_id(request=json_request, storeId="storeId", query_parameters=query_parameters)
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   request_body = {
     :data => ''
   }

   query_params = {
     :model => 'string'
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_logo_by_store_id(request_body, 'storeId', query_params: query_params)
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request objects
   const logo: Types.management.Logo = {
   	data: ""
   };

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalLogoByStoreId("storeId", logo, "string");
   ```

   The response returns the logo inherited from a higher level:

   **Response showing current logo**

   ```json
   {
     "data": "R0lGODlh...AAAcDgH+BADs="
   }
   ```

### Update other settings

1. Make sure that you know:

   * The [level](#inheritance) where you need to add or remove settings to get the desired effect.

   * The [current settings](#view-current-settings) at that level. When removing settings, you also need to know the settings that will be inherited from the next higher level.

2. To update settings (except the terminal logo), make a PATCH request to the `/terminalSettings` endpoint for the account, store, or terminal.

   Use the following endpoints:

   * PATCH [/companies/{companyId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/patch/companies/\(companyId\)/terminalSettings)
   * PATCH [/merchants/{merchantId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/patch/merchants/\(merchantId\)/terminalSettings)
   * PATCH [/stores/{storeId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/patch/stores/\(storeId\)/terminalSettings)
   * PATCH [/terminals/{terminalId}/terminalSettings](https://docs.adyen.com/api-explorer/Management/latest/patch/terminals/\(terminalId\)/terminalSettings)

   In the request body, specify the full objects for the settings that you want to change.

   If you omit a parameter from an object, that setting is removed.

   Remote Wi-Fi profiles require many different parameters depending on the type or of network: Enterprise EAP-PEAP, Enterprise EAP-TLS, or Personal PSK. For details, see [Set up Wi-Fi profiles using API calls](/point-of-sale/design-your-integration/network-and-connectivity/wi-fi-profiles#set-up-with-api).

   The next example shows a request that does two things:

   * You previously defined tipping settings for a store but now want to remove those and go back to the tipping settings from the merchant level.
   * You want to change the `skipSignature` setting for the store from **true** to **false**.

   **Update store settings**

   #### curl

   ```bash
   curl https://management-test.adyen.com/v3/stores/{storeId}/terminalSettings \
   -H 'x-API-key: ADYEN_API_KEY' \
   -X PATCH \
   -d '{
        "gratuities": [
          {
            "currency": "",
            "usePredefinedTipEntries": "",
            "predefinedTipEntries": [],
            "allowCustomAmount": ""
          }
        ],
        "signatureSettings": {
           "askSignatureOnScreen": true,
           "skipSignature": false,
           "deviceName": "Paris1-12"
         }
   }'
   ```

   #### Java

   ```java
   // Adyen Java API Library v25.0.0
   import com.adyen.Client;
   import com.adyen.enums.Environment;
   import com.adyen.model.management.*;
   import java.time.OffsetDateTime;
   import java.util.*;
   import com.adyen.service.management.*;

   Client client = new Client("ADYEN_API_KEY", Environment.TEST);

   // Request objects
   Gratuity gratuity1 = new Gratuity()
   	.predefinedTipEntries(Arrays.asList())
   	.allowCustomAmount()
   	.usePredefinedTipEntries()
   	.currency("");

   TerminalSettings terminalSettings = new TerminalSettings()
   	.gratuities(Arrays.asList(gratuity1));

   // Make the request
   TerminalSettingsStoreLevelApi service = new TerminalSettingsStoreLevelApi(client);
   TerminalSettings response = service.updateTerminalSettingsByStoreId("storeId", terminalSettings, null);
   ```

   #### PHP

   ```php
   // Adyen PHP API Library v17.4.0
   use Adyen\Client;
   use Adyen\Environment;
   use Adyen\Model\Management\Gratuity;
   use Adyen\Model\Management\TerminalSettings;
   use Adyen\Service\Management\TerminalSettingsStoreLevelApi;

   $client = new Client();
   $client->setXApiKey("ADYEN_API_KEY");
   $client->setEnvironment(Environment::TEST);


   // Request objects
   $gratuity1 = new Gratuity();
   $gratuity1
   	->setPredefinedTipEntries(array())
   	->setAllowCustomAmount()
   	->setUsePredefinedTipEntries()
   	->setCurrency("");

   $terminalSettings = new TerminalSettings();
   $terminalSettings
   	->setGratuities(array($gratuity1));

   // Make the request
   $service = new TerminalSettingsStoreLevelApi($client);
   $response = $service->updateTerminalSettingsByStoreId('storeId', $terminalSettings);
   ```

   #### C\#

   ```cs
   // Adyen .net API Library v14.3.0
   using Adyen;
   using Environment = Adyen.Model.Environment;
   using Adyen.Model;
   using Adyen.Model.Management;
   using Adyen.Service.Management;

   var config = new Config()
   {
       XApiKey = "ADYEN_API_KEY",
       Environment = Environment.Test
   };
   var client = new Client(config);

   // Fill in your request objects
   Gratuity gratuity1 = new Gratuity
   {
   	PredefinedTipEntries = {  },
   	AllowCustomAmount = ,
   	UsePredefinedTipEntries = ,
   	Currency = ""
   };

   TerminalSettings terminalSettings = new TerminalSettings
   {
   	Gratuities = new List<Gratuity>{ gratuity1 }
   };

   // Make the request
   var service = new TerminalSettingsStoreLevelService(client);
   var response = service.UpdateTerminalSettingsByStoreId("storeId", terminalSettings);
   ```

   #### NodeJS (JavaScript)

   ```js
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   const { Client, ManagementAPI } = require('@adyen/api-library');

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request object
   const terminalSettings = {
     gratuities: [ {
       currency: "",
       usePredefinedTipEntries: "",
       predefinedTipEntries: [ ],
       allowCustomAmount: ""
     } ],
     signatureSettings: {
       askSignatureOnScreen: true,
       skipSignature: false,
       deviceName: "Paris1-12"
     }
   }

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalSettingsByStoreId("storeId", terminalSettings);
   ```

   #### Go

   ```go
   // Adyen Go API Library v9.2.0
   import (
   	"context"
   	"github.com/adyen/adyen-go-api-library/v9/src/common"
   	"github.com/adyen/adyen-go-api-library/v9/src/adyen"
   	"github.com/adyen/adyen-go-api-library/v9/src/management"
   )

   client := adyen.NewClient(&common.Config{
       ApiKey:      "ADYEN_API_KEY",
       Environment: common.TestEnv,
   })

   // Fill in your request objects
   gratuity1 := management.Gratuity{
   	PredefinedTipEntries: []string{
   		,
   	},
   	AllowCustomAmount: common.PtrBool(),
   	UsePredefinedTipEntries: common.PtrBool(),
   	Currency: common.PtrString(""),
   }

   terminalSettings := management.TerminalSettings{
   	Gratuities: []management.Gratuity{
   			gratuity1,
   	},
   }

   // Make the request
   service := client.Management()
   req := service.TerminalSettingsStoreLevelApi.UpdateTerminalSettingsByStoreIdInput("storeId").TerminalSettings(terminalSettings)
   res, httpRes, err := service.TerminalSettingsStoreLevelApi.UpdateTerminalSettingsByStoreId(context.Background(), req)
   ```

   #### Python

   ```py
   # Adyen Python API Library v12.2.0
   import Adyen

   adyen = Adyen.Adyen()
   adyen.client.xapikey = "ADYEN_API_KEY"
   adyen.client.platform = "test" # The environment to use library in.

   json_request = {
     "gratuities": [ {
       "currency": "",
       "usePredefinedTipEntries": "",
       "predefinedTipEntries": [ ],
       "allowCustomAmount": ""
     } ],
     "signatureSettings": {
       "askSignatureOnScreen": True,
       "skipSignature": False,
       "deviceName": "Paris1-12"
     }
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_settings_by_store_id(request=json_request, storeId="storeId")
   ```

   #### Ruby

   ```rb
   # Adyen Ruby API Library v9.2.0
   require "adyen-ruby-api-library"

   adyen = Adyen::Client.new
   adyen.api_key = 'ADYEN_API_KEY'
   adyen.env = :test # Set to "live" for live environment

   request_body = {
     :gratuities => [ {
       :currency => '',
       :usePredefinedTipEntries => '',
       :predefinedTipEntries => [ ],
       :allowCustomAmount => ''
     } ],
     :signatureSettings => {
       :askSignatureOnScreen => true,
       :skipSignature => false,
       :deviceName => 'Paris1-12'
     }
   }

   result = adyen.management.terminal_settings_store_level_api.update_terminal_settings_by_store_id(request_body, 'storeId')
   ```

   #### NodeJS (TypeScript)

   ```ts
   // Adyen Node API Library v16.2.0
   // Require the parts of the module you want to use
   import { Client, ManagementAPI, Types } from "@adyen/api-library";

   // Initialize the client object
   const client = new Client({apiKey: "ADYEN_API_KEY", environment: "TEST"});

   // Create the request objects
   const gratuity1: Types.management.Gratuity = {
   	predefinedTipEntries: [],
   	allowCustomAmount: ,
   	usePredefinedTipEntries: ,
   	currency: ""
   };

   const terminalSettings: Types.management.TerminalSettings = {
   	gratuities: [gratuity1]
   };

   // Make the request
   const managementAPI = new ManagementAPI(client);
   const response = managementAPI.TerminalSettingsStoreLevelApi.updateTerminalSettingsByStoreId("storeId", terminalSettings);
   ```

   The response returns the entire configuration at the level where you made the request.

## When new settings take effect

The terminal downloads the configuration changes when it makes a maintenance call to our systems. This happens automatically every 180 minutes. The configuration changes then take effect when the terminal restarts. This happens automatically at the restart hour, which is usually set to 6:00 AM.

To switch to the new passcode immediately, update the configuration on your terminal manually:

* Go to **Settings** > **Configuration**, enter your [Admin & Settings passcode](/point-of-sale/menu-access/#manage-passcodes) and select **Update**.

* For *non-Android terminals* that use the [old menu structure](/point-of-sale/menu-access#old-menu-structure): go to the [Admin menu](/point-of-sale/pos-troubleshooting/terminal-turns-on/admin-pin#open-admin-menu) and select **Config** > **Update**.

The terminal downloads the updated configuration and then restarts.

If you ordered new terminals and want to update the settings at terminal level, you can configure the new terminals as soon as they are shipped. You receive an email when this happens, and you can check your [Terminal order update](/point-of-sale/managing-terminals/order-terminals#terminal-order-notifications) webhooks. Or you can go to your [Customer Area](https://ca-test.adyen.com/) > **Orders and returns** and make sure that the order is

Shipped

or

Delivered

.

On new terminals, the settings take effect when the terminal is [boarded](/point-of-sale/managing-terminals/board-terminal).

## See also

* [Order terminals using API requests](/point-of-sale/automating-terminal-management/order-terminals-api)
* [Assign terminals using API requests](/point-of-sale/automating-terminal-management/assign-terminals-api)
* [Set up Wi-Fi profiles using API calls](/point-of-sale/design-your-integration/network-and-connectivity/wi-fi-profiles#set-up-with-api)
* [Configure surcharges using API calls](/point-of-sale/surcharge#configure-surcharges)
* [Manage cellular connectivity using API calls](/point-of-sale/design-your-integration/network-and-connectivity/cellular-failover#set-up-cellular-connectivity-on-the-terminal)
